setenv : setenv()
         unsetenv()

       Changes or adds an environment variable.

       The setenv() function adds the variable 'name' to the
       environment with the value 'value', if 'name' does not
       already exist.
       The unsetenv() function deletes the variable 'name'
       to the environment.

       Return Value
       setenv() and unsetenv() returns 0 on success or -1 on
       error.
